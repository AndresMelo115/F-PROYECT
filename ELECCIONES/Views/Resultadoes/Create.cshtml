@model ELECCIONES.Models.Resultado

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Resultado</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdElecciones" class="control-label"></label>
                <select asp-for="IdElecciones" class ="form-control" asp-items="ViewBag.IdElecciones"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdCandidatos" class="control-label"></label>
                <select asp-for="IdCandidatos" class ="form-control" asp-items="ViewBag.IdCandidatos"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdCiudadanos" class="control-label"></label>
                <select asp-for="IdCiudadanos" class ="form-control" asp-items="ViewBag.IdCiudadanos"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

